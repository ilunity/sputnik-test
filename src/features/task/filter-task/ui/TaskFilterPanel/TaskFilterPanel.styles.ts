import { Space, Typography } from 'antd';
import { styled } from 'styled-components';

const { Text } = Typography;

export const FiltersContainer = styled(Space)`
    min-width: 100%;
`;

export const PanelTitle = styled(Text)`
     font-size: 16px;
 `;
